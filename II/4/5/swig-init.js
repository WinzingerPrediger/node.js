var swig 		= require('swig');
var options 	= {
		// ...
	};

swig.setDefaults( options );

/*
swig.setDefaults( {
	autoescape:	true,
	cache: 		( conf.type == "dev" ? false : "memory" ),
	loader: 	swig.loaders.fs( _dirname )
});
*/
